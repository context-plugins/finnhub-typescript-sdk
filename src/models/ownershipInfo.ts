/**
 * Finnhub APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  bigint,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

export interface OwnershipInfo {
  /** Investor's name. */
  name?: string;
  /** Number of shares held by the investor. */
  share?: bigint;
  /** Number of share changed (net buy or sell) from the last period. */
  change?: bigint;
  /** Filing date. */
  filingDate?: string;
  additionalProperties?: Record<string, unknown>;
}

export const ownershipInfoSchema: Schema<OwnershipInfo> = typedExpandoObject(
  {
    name: ['name', optional(string())],
    share: ['share', optional(bigint())],
    change: ['change', optional(bigint())],
    filingDate: ['filingDate', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
