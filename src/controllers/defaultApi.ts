/**
 * Finnhub APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AggregateIndicators,
  aggregateIndicatorsSchema,
} from '../models/aggregateIndicators.js';
import { AiChatBody, aiChatBodySchema } from '../models/aiChatBody.js';
import {
  AiChatResponse,
  aiChatResponseSchema,
} from '../models/aiChatResponse.js';
import {
  AirlinePriceIndexData,
  airlinePriceIndexDataSchema,
} from '../models/airlinePriceIndexData.js';
import { BankBranchRes, bankBranchResSchema } from '../models/bankBranchRes.js';
import {
  BasicFinancials,
  basicFinancialsSchema,
} from '../models/basicFinancials.js';
import { BondCandles, bondCandlesSchema } from '../models/bondCandles.js';
import { BondProfile, bondProfileSchema } from '../models/bondProfile.js';
import { BondTickData, bondTickDataSchema } from '../models/bondTickData.js';
import {
  BondYieldCurve,
  bondYieldCurveSchema,
} from '../models/bondYieldCurve.js';
import {
  CapexEstimates,
  capexEstimatesSchema,
} from '../models/capexEstimates.js';
import {
  CompanyEarningsQualityScore,
  companyEarningsQualityScoreSchema,
} from '../models/companyEarningsQualityScore.js';
import { CompanyEsg, companyEsgSchema } from '../models/companyEsg.js';
import {
  CompanyExecutive,
  companyExecutiveSchema,
} from '../models/companyExecutive.js';
import { CompanyNews, companyNewsSchema } from '../models/companyNews.js';
import {
  CompanyProfile,
  companyProfileSchema,
} from '../models/companyProfile.js';
import {
  CompanyProfile2,
  companyProfile2Schema,
} from '../models/companyProfile2.js';
import {
  CongressionalTrading,
  congressionalTradingSchema,
} from '../models/congressionalTrading.js';
import {
  CountryMetadata,
  countryMetadataSchema,
} from '../models/countryMetadata.js';
import { CovidInfo, covidInfoSchema } from '../models/covidInfo.js';
import { CryptoCandles, cryptoCandlesSchema } from '../models/cryptoCandles.js';
import { CryptoProfile, cryptoProfileSchema } from '../models/cryptoProfile.js';
import { CryptoSymbol, cryptoSymbolSchema } from '../models/cryptoSymbol.js';
import { Dividends, dividendsSchema } from '../models/dividends.js';
import { Dividends2, dividends2Schema } from '../models/dividends2.js';
import { DpsEstimates, dpsEstimatesSchema } from '../models/dpsEstimates.js';
import { EarningResult, earningResultSchema } from '../models/earningResult.js';
import {
  EarningsCalendar,
  earningsCalendarSchema,
} from '../models/earningsCalendar.js';
import {
  EarningsCallLive,
  earningsCallLiveSchema,
} from '../models/earningsCallLive.js';
import {
  EarningsCallTranscripts,
  earningsCallTranscriptsSchema,
} from '../models/earningsCallTranscripts.js';
import {
  EarningsCallTranscriptsList,
  earningsCallTranscriptsListSchema,
} from '../models/earningsCallTranscriptsList.js';
import {
  EarningsEstimates,
  earningsEstimatesSchema,
} from '../models/earningsEstimates.js';
import {
  EbitdaEstimates,
  ebitdaEstimatesSchema,
} from '../models/ebitdaEstimates.js';
import { EbitEstimates, ebitEstimatesSchema } from '../models/ebitEstimates.js';
import {
  EconomicCalendar,
  economicCalendarSchema,
} from '../models/economicCalendar.js';
import { EconomicCode, economicCodeSchema } from '../models/economicCode.js';
import { EconomicData, economicDataSchema } from '../models/economicData.js';
import {
  EtFsAllocation,
  etFsAllocationSchema,
} from '../models/etFsAllocation.js';
import {
  EtFsCountryExposure,
  etFsCountryExposureSchema,
} from '../models/etFsCountryExposure.js';
import { EtFsHoldings, etFsHoldingsSchema } from '../models/etFsHoldings.js';
import { EtFsProfile, etFsProfileSchema } from '../models/etFsProfile.js';
import {
  EtFsSectorExposure,
  etFsSectorExposureSchema,
} from '../models/etFsSectorExposure.js';
import { FcfEstimates, fcfEstimatesSchema } from '../models/fcfEstimates.js';
import {
  FdaComitteeMeeting,
  fdaComitteeMeetingSchema,
} from '../models/fdaComitteeMeeting.js';
import { Filing, filingSchema } from '../models/filing.js';
import {
  FinancialsAsReported,
  financialsAsReportedSchema,
} from '../models/financialsAsReported.js';
import {
  FinancialStatements,
  financialStatementsSchema,
} from '../models/financialStatements.js';
import { ForexCandles, forexCandlesSchema } from '../models/forexCandles.js';
import { Forexrates, forexratesSchema } from '../models/forexrates.js';
import { ForexSymbol, forexSymbolSchema } from '../models/forexSymbol.js';
import { FundOwnership, fundOwnershipSchema } from '../models/fundOwnership.js';
import {
  GrossIncomeEstimates,
  grossIncomeEstimatesSchema,
} from '../models/grossIncomeEstimates.js';
import {
  HistoricalCompanyEsg,
  historicalCompanyEsgSchema,
} from '../models/historicalCompanyEsg.js';
import {
  HistoricalEmployeeCount,
  historicalEmployeeCountSchema,
} from '../models/historicalEmployeeCount.js';
import {
  HistoricalMarketCapData,
  historicalMarketCapDataSchema,
} from '../models/historicalMarketCapData.js';
import {
  HistoricalNbbo,
  historicalNbboSchema,
} from '../models/historicalNbbo.js';
import {
  IndicesConstituents,
  indicesConstituentsSchema,
} from '../models/indicesConstituents.js';
import {
  IndicesHistoricalConstituents,
  indicesHistoricalConstituentsSchema,
} from '../models/indicesHistoricalConstituents.js';
import {
  InFilingResponse,
  inFilingResponseSchema,
} from '../models/inFilingResponse.js';
import {
  InFilingSearchBody,
  inFilingSearchBodySchema,
} from '../models/inFilingSearchBody.js';
import {
  InsiderSentiments,
  insiderSentimentsSchema,
} from '../models/insiderSentiments.js';
import {
  InsiderTransactions,
  insiderTransactionsSchema,
} from '../models/insiderTransactions.js';
import {
  InstitutionalOwnership,
  institutionalOwnershipSchema,
} from '../models/institutionalOwnership.js';
import {
  InstitutionalPortfolio,
  institutionalPortfolioSchema,
} from '../models/institutionalPortfolio.js';
import {
  InstitutionalProfile,
  institutionalProfileSchema,
} from '../models/institutionalProfile.js';
import {
  InternationalFiling,
  internationalFilingSchema,
} from '../models/internationalFiling.js';
import {
  InvestmentThemes,
  investmentThemesSchema,
} from '../models/investmentThemes.js';
import { IpoCalendar, ipoCalendarSchema } from '../models/ipoCalendar.js';
import { IsinChange, isinChangeSchema } from '../models/isinChange.js';
import { LastBidAsk, lastBidAskSchema } from '../models/lastBidAsk.js';
import {
  LobbyingResult,
  lobbyingResultSchema,
} from '../models/lobbyingResult.js';
import { MarketHoliday, marketHolidaySchema } from '../models/marketHoliday.js';
import { MarketNews, marketNewsSchema } from '../models/marketNews.js';
import { MarketStatus, marketStatusSchema } from '../models/marketStatus.js';
import {
  MutualFundCountryExposure,
  mutualFundCountryExposureSchema,
} from '../models/mutualFundCountryExposure.js';
import { MutualFundEet, mutualFundEetSchema } from '../models/mutualFundEet.js';
import {
  MutualFundEetPai,
  mutualFundEetPaiSchema,
} from '../models/mutualFundEetPai.js';
import {
  MutualFundHoldings,
  mutualFundHoldingsSchema,
} from '../models/mutualFundHoldings.js';
import {
  MutualFundProfile,
  mutualFundProfileSchema,
} from '../models/mutualFundProfile.js';
import {
  MutualFundSectorExposure,
  mutualFundSectorExposureSchema,
} from '../models/mutualFundSectorExposure.js';
import {
  NetIncomeEstimates,
  netIncomeEstimatesSchema,
} from '../models/netIncomeEstimates.js';
import { Newsroom, newsroomSchema } from '../models/newsroom.js';
import { NewsSentiment, newsSentimentSchema } from '../models/newsSentiment.js';
import { OcfEstimates, ocfEstimatesSchema } from '../models/ocfEstimates.js';
import { Ownership, ownershipSchema } from '../models/ownership.js';
import {
  PatternRecognition,
  patternRecognitionSchema,
} from '../models/patternRecognition.js';
import { PressRelease, pressReleaseSchema } from '../models/pressRelease.js';
import {
  PretaxIncomeEstimates,
  pretaxIncomeEstimatesSchema,
} from '../models/pretaxIncomeEstimates.js';
import { PriceMetrics, priceMetricsSchema } from '../models/priceMetrics.js';
import { PriceTarget, priceTargetSchema } from '../models/priceTarget.js';
import { Quote, quoteSchema } from '../models/quote.js';
import {
  RecommendationTrend,
  recommendationTrendSchema,
} from '../models/recommendationTrend.js';
import {
  RevenueBreakdown,
  revenueBreakdownSchema,
} from '../models/revenueBreakdown.js';
import {
  RevenueBreakdown2,
  revenueBreakdown2Schema,
} from '../models/revenueBreakdown2.js';
import {
  RevenueEstimates,
  revenueEstimatesSchema,
} from '../models/revenueEstimates.js';
import { SearchBody, searchBodySchema } from '../models/searchBody.js';
import { SearchFilter, searchFilterSchema } from '../models/searchFilter.js';
import {
  SearchResponse,
  searchResponseSchema,
} from '../models/searchResponse.js';
import {
  SecSentimentAnalysis,
  secSentimentAnalysisSchema,
} from '../models/secSentimentAnalysis.js';
import { SectorMetric, sectorMetricSchema } from '../models/sectorMetric.js';
import {
  SimilarityIndex,
  similarityIndexSchema,
} from '../models/similarityIndex.js';
import {
  SocialSentiment,
  socialSentimentSchema,
} from '../models/socialSentiment.js';
import { Split, splitSchema } from '../models/split.js';
import { StockCandles, stockCandlesSchema } from '../models/stockCandles.js';
import {
  StockPresentation,
  stockPresentationSchema,
} from '../models/stockPresentation.js';
import { StockSymbol, stockSymbolSchema } from '../models/stockSymbol.js';
import {
  SupplyChainRelationships,
  supplyChainRelationshipsSchema,
} from '../models/supplyChainRelationships.js';
import {
  SupportResistance,
  supportResistanceSchema,
} from '../models/supportResistance.js';
import { SymbolChange, symbolChangeSchema } from '../models/symbolChange.js';
import { SymbolLookup, symbolLookupSchema } from '../models/symbolLookup.js';
import { TickData, tickDataSchema } from '../models/tickData.js';
import {
  UpgradeDowngrade,
  upgradeDowngradeSchema,
} from '../models/upgradeDowngrade.js';
import {
  UsaSpendingResult,
  usaSpendingResultSchema,
} from '../models/usaSpendingResult.js';
import {
  UsptoPatentResult,
  usptoPatentResultSchema,
} from '../models/usptoPatentResult.js';
import {
  VisaApplicationResult,
  visaApplicationResultSchema,
} from '../models/visaApplicationResult.js';
import {
  array,
  bigint,
  boolean,
  optional,
  string,
  unknown,
} from '../schema.js';
import { BaseApi } from './baseApi.js';

export class DefaultApi extends BaseApi {
  /**
   * Search for best-matching symbols based on your query. You can input anything from symbol, security's
   * name to ISIN and Cusip.
   *
   * @param q            Query text can be symbol, name, isin, or cusip.
   * @param exchange     Exchange limit.
   * @return Response from the API call
   */
  async symbolSearch(
    q: string,
    exchange?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SymbolLookup>> {
    const req = this.createRequest('GET', '/search');
    const mapped = req.prepareArgs({
      q: [q, string()],
      exchange: [exchange, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('q', mapped.q);
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(symbolLookupSchema, requestOptions);
  }

  /**
   * List supported stocks. We use the following symbology to identify stocks on Finnhub
   * <code>Exchange_Ticker.Exchange_Code</code>. A list of supported exchange codes can be found <a
   * href="https://docs.google.com/spreadsheets/d/1I3pBxjfXB056-g_JYf_6o3Rns3BV2kMGG1nCatb91ls/edit?
   * usp=sharing" target="_blank">here</a>.
   *
   * @param exchange     Exchange you want to get the list of symbols from. List of exchange codes can be
   *                               found <a href="https://docs.google.com/spreadsheets/d/1I3pBxjfXB056-
   *                               g_JYf_6o3Rns3BV2kMGG1nCatb91ls/edit?usp=sharing" target="_blank">here</a>.
   * @param mic          Filter by MIC code.
   * @param securityType Filter by security type used by OpenFigi standard.
   * @param currency     Filter by currency.
   * @return Response from the API call
   */
  async stockSymbols(
    exchange: string,
    mic?: string,
    securityType?: string,
    currency?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<StockSymbol[]>> {
    const req = this.createRequest('GET', '/stock/symbol');
    const mapped = req.prepareArgs({
      exchange: [exchange, string()],
      mic: [mic, optional(string())],
      securityType: [securityType, optional(string())],
      currency: [currency, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('exchange', mapped.exchange);
    req.query('mic', mapped.mic);
    req.query('securityType', mapped.securityType);
    req.query('currency', mapped.currency);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(stockSymbolSchema), requestOptions);
  }

  /**
   * Get current market status for global exchanges (whether exchanges are open or close).
   *
   * @param exchange     Exchange code.
   * @return Response from the API call
   */
  async marketStatus(
    exchange: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MarketStatus>> {
    const req = this.createRequest('GET', '/stock/market-status');
    const mapped = req.prepareArgs({ exchange: [exchange, string()] });
    req.header('Content-Type', 'application/json');
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(marketStatusSchema, requestOptions);
  }

  /**
   * Get a list of holidays for global exchanges.
   *
   * @param exchange     Exchange code.
   * @return Response from the API call
   */
  async marketHoliday(
    exchange: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MarketHoliday>> {
    const req = this.createRequest('GET', '/stock/market-holiday');
    const mapped = req.prepareArgs({ exchange: [exchange, string()] });
    req.header('Content-Type', 'application/json');
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(marketHolidaySchema, requestOptions);
  }

  /**
   * Get general information of a company. You can query by symbol, ISIN or CUSIP
   *
   * @param symbol       Symbol of the company: AAPL e.g.
   * @param isin         ISIN
   * @param cusip        CUSIP
   * @return Response from the API call
   */
  async companyProfile(
    symbol?: string,
    isin?: string,
    cusip?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyProfile>> {
    const req = this.createRequest('GET', '/stock/profile');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
      cusip: [cusip, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.query('cusip', mapped.cusip);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(companyProfileSchema, requestOptions);
  }

  /**
   * Get general information of a company. You can query by symbol, ISIN or CUSIP. This is the free
   * version of <a href="#company-profile">Company Profile</a>.
   *
   * @param symbol       Symbol of the company: AAPL e.g.
   * @param isin         ISIN
   * @param cusip        CUSIP
   * @return Response from the API call
   */
  async companyProfile2(
    symbol?: string,
    isin?: string,
    cusip?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyProfile2>> {
    const req = this.createRequest('GET', '/stock/profile2');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
      cusip: [cusip, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.query('cusip', mapped.cusip);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(companyProfile2Schema, requestOptions);
  }

  /**
   * Get a list of company's executives and members of the Board.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @return Response from the API call
   */
  async companyExecutive(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyExecutive>> {
    const req = this.createRequest('GET', '/stock/executive');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(companyExecutiveSchema, requestOptions);
  }

  /**
   * Get latest market news.
   *
   * @param category     This parameter can be 1 of the following values <code>general, forex, crypto,
   *                               merger</code>.
   * @param minId        Use this field to get only news after this ID. Default to 0
   * @return Response from the API call
   */
  async marketNews(
    category: string,
    minId?: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MarketNews[]>> {
    const req = this.createRequest('GET', '/news');
    const mapped = req.prepareArgs({
      category: [category, string()],
      minId: [minId, optional(bigint())],
    });
    req.header('Content-Type', 'application/json');
    req.query('category', mapped.category);
    req.query('minId', mapped.minId);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(marketNewsSchema), requestOptions);
  }

  /**
   * List latest company news by symbol. This endpoint is only available for North American companies.
   *
   * @param symbol       Company symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async companyNews(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyNews[]>> {
    const req = this.createRequest('GET', '/company-news');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(companyNewsSchema), requestOptions);
  }

  /**
   * <p>Get latest major press releases of a company. This data can be used to highlight the most
   * significant events comprised of mostly press releases sourced from the exchanges, BusinessWire,
   * AccessWire, GlobeNewswire, Newsfile, and PRNewswire.</p><p>Full-text press releases data is
   * available for Enterprise clients. <a href="mailto:support@finnhub.io">Contact Us</a> to learn more.
   * </p>
   *
   * @param symbol       Company symbol.
   * @param from         From time: 2020-01-01.
   * @param to           To time: 2020-01-05.
   * @return Response from the API call
   */
  async pressReleases(
    symbol: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PressRelease>> {
    const req = this.createRequest('GET', '/press-releases');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(pressReleaseSchema, requestOptions);
  }

  /**
   * Get company's news sentiment and statistics. This endpoint is only available for US companies.
   *
   * @param symbol       Company symbol.
   * @return Response from the API call
   */
  async newsSentiment(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NewsSentiment>> {
    const req = this.createRequest('GET', '/news-sentiment');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(newsSentimentSchema, requestOptions);
  }

  /**
   * Get company peers. Return a list of peers operating in the same country and sector/industry.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param grouping     Specify the grouping criteria for choosing peers.Supporter values:
   *                               <code>sector</code>, <code>industry</code>, <code>subIndustry</code>. Default to
   *                               <code>subIndustry</code>.
   * @return Response from the API call
   */
  async companyPeers(
    symbol: string,
    grouping?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string[]>> {
    const req = this.createRequest('GET', '/stock/peers');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      grouping: [grouping, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('grouping', mapped.grouping);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(string()), requestOptions);
  }

  /**
   * Get company basic financials such as margin, P/E ratio, 52-week high/low etc.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param metric       Metric type. Can be 1 of the following values <code>all</code>
   * @return Response from the API call
   */
  async companyBasicFinancials(
    symbol: string,
    metric: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BasicFinancials>> {
    const req = this.createRequest('GET', '/stock/metric');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      metric: [metric, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('metric', mapped.metric);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(basicFinancialsSchema, requestOptions);
  }

  /**
   * Get company price performance statistics such as 52-week high/low, YTD return and much more.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param date         Get data on a specific date in the past. The data is available weekly so your date
   *                               will be automatically adjusted to the last day of that week.
   * @return Response from the API call
   */
  async priceMetrics(
    symbol: string,
    date?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PriceMetrics>> {
    const req = this.createRequest('GET', '/stock/price-metric');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      date: [date, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('date', mapped.date);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(priceMetricsSchema, requestOptions);
  }

  /**
   * Get a list of symbol changes for US-listed, EU-listed, NSE and ASX securities. Limit to 2000 events
   * at a time.
   *
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async symbolChange(
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SymbolChange>> {
    const req = this.createRequest('GET', '/ca/symbol-change');
    const mapped = req.prepareArgs({
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(symbolChangeSchema, requestOptions);
  }

  /**
   * Get a list of ISIN changes for EU-listed securities. Limit to 2000 events at a time.
   *
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async isinChange(
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IsinChange>> {
    const req = this.createRequest('GET', '/ca/isin-change');
    const mapped = req.prepareArgs({
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(isinChangeSchema, requestOptions);
  }

  /**
   * Get historical market cap data for global companies.
   *
   * @param symbol       Company symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async historicalMarketCap(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoricalMarketCapData>> {
    const req = this.createRequest('GET', '/stock/historical-market-cap');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(historicalMarketCapDataSchema, requestOptions);
  }

  /**
   * Get historical employee count for global companies.
   *
   * @param symbol       Company symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async historicalEmployeeCount(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoricalEmployeeCount>> {
    const req = this.createRequest('GET', '/stock/historical-employee-count');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(historicalEmployeeCountSchema, requestOptions);
  }

  /**
   * Get a list of well-known institutional investors. Currently support 60+ profiles.
   *
   * @param cik          Filter by CIK. Leave blank to get the full list.
   * @return Response from the API call
   */
  async institutionalProfile(
    cik?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionalProfile>> {
    const req = this.createRequest('GET', '/institutional/profile');
    const mapped = req.prepareArgs({ cik: [cik, optional(string())] });
    req.header('Content-Type', 'application/json');
    req.query('cik', mapped.cik);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(institutionalProfileSchema, requestOptions);
  }

  /**
   * Get the holdings/portfolio data of institutional investors from 13-F filings. Limit to 1 year of
   * data at a time. You can get a list of supported CIK <a href="/api/v1/institutional/list?token="
   * target="_blank">here</a>.
   *
   * @param cik          Fund's CIK.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async institutionalPortfolio(
    cik: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionalPortfolio>> {
    const req = this.createRequest('GET', '/institutional/portfolio');
    const mapped = req.prepareArgs({
      cik: [cik, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('cik', mapped.cik);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(institutionalPortfolioSchema, requestOptions);
  }

  /**
   * Get a list institutional investors' positions for a particular stock overtime. Data from 13-F
   * filings. Limit to 1 year of data at a time.
   *
   * @param symbol       Filter by symbol.
   * @param cusip        Filter by CUSIP.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async institutionalOwnership(
    symbol: string,
    cusip: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionalOwnership>> {
    const req = this.createRequest('GET', '/institutional/ownership');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      cusip: [cusip, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('cusip', mapped.cusip);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(institutionalOwnershipSchema, requestOptions);
  }

  /**
   * Get a full list of shareholders of a company in descending order of the number of shares held. Data
   * is sourced from <code>13F form</code>, <code>Schedule 13D</code> and <code>13G</code> for US market,
   * <code>UK Share Register</code> for UK market, <code>SEDI</code> for Canadian market and equivalent
   * filings for other international markets.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param limit        Limit number of results. Leave empty to get the full list.
   * @return Response from the API call
   */
  async ownership(
    symbol: string,
    limit?: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Ownership>> {
    const req = this.createRequest('GET', '/stock/ownership');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      limit: [limit, optional(bigint())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('limit', mapped.limit);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(ownershipSchema, requestOptions);
  }

  /**
   * Get a full list fund and institutional investors of a company in descending order of the number of
   * shares held. Data is sourced from <code>13F form</code>, <code>Schedule 13D</code> and
   * <code>13G</code> for US market, <code>UK Share Register</code> for UK market, <code>SEDI</code> for
   * Canadian market and equivalent filings for other international markets.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param limit        Limit number of results. Leave empty to get the full list.
   * @return Response from the API call
   */
  async fundOwnership(
    symbol: string,
    limit?: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<FundOwnership>> {
    const req = this.createRequest('GET', '/stock/fund-ownership');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      limit: [limit, optional(bigint())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('limit', mapped.limit);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(fundOwnershipSchema, requestOptions);
  }

  /**
   * Company insider transactions data sourced from <code>Form 3,4,5</code>, SEDI and relevant companies'
   * filings. This endpoint covers US, UK, Canada, Australia, India, and all major EU markets. Limit to
   * 100 transactions per API call.
   *
   * @param symbol       Symbol of the company: AAPL. Leave this param blank to get the latest transactions.
   * @param from         From date: 2020-03-15.
   * @param to           To date: 2020-03-16.
   * @return Response from the API call
   */
  async insiderTransactions(
    symbol: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InsiderTransactions>> {
    const req = this.createRequest('GET', '/stock/insider-transactions');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(insiderTransactionsSchema, requestOptions);
  }

  /**
   * Get insider sentiment data for US companies calculated using method discussed <a href="https:
   * //medium.com/@stock-api/finnhub-insiders-sentiment-analysis-cc43f9f64b3a" target="_blank">here</a>.
   * The MSPR ranges from -100 for the most negative to 100 for the most positive which can signal price
   * changes in the coming 30-90 days.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param from         From date: 2020-03-15.
   * @param to           To date: 2020-03-16.
   * @return Response from the API call
   */
  async insiderSentiment(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InsiderSentiments>> {
    const req = this.createRequest('GET', '/stock/insider-sentiment');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(insiderSentimentsSchema, requestOptions);
  }

  /**
   * <p>Get standardized balance sheet, income statement and cash flow for global companies going back
   * 30+ years. Data is sourced from original filings most of which made available through <a
   * href="#filings">SEC Filings</a> and <a href="#international-filings">International Filings</a>
   * endpoints.</p><p>Set <code>preliminary</code> param to true for faster updates for US companies.
   * </p><p><i>Wondering why our standardized data is different from Bloomberg, Reuters, Factset, S&P or
   * Yahoo Finance ? Check out our <a href="/faq">FAQ page</a> to learn more</i></p>
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param statement    Statement can take 1 of these values <code>bs, ic, cf</code> for Balance Sheet,
   *                               Income Statement, Cash Flow respectively.
   * @param freq         Frequency can take 1 of these values <code>annual, quarterly, ttm, ytd</code>.  TTM
   *                               (Trailing Twelve Months) option is available for Income Statement and Cash Flow. YTD
   *                               (Year To Date) option is only available for Cash Flow.
   * @param preliminary  If set to <code>true</code>, it will return Preliminary financial statements for
   *                               the latest period which are usually available within an hour of the earnings
   *                               announcement if finalized data is not available yet. This preliminary data is
   *                               currently available for US companies. You will see <code>"preliminary": true</code>
   *                               in the data if that period is using preliminary data.
   * @return Response from the API call
   */
  async financials(
    symbol: string,
    statement: string,
    freq: string,
    preliminary?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<FinancialStatements>> {
    const req = this.createRequest('GET', '/stock/financials');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      statement: [statement, string()],
      freq: [freq, string()],
      preliminary: [preliminary, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('statement', mapped.statement);
    req.query('freq', mapped.freq);
    req.query('preliminary', mapped.preliminary);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(financialStatementsSchema, requestOptions);
  }

  /**
   * Get financials as reported. This data is available for bulk download on <a href="https://www.kaggle.
   * com/finnhub/reported-financials" target="_blank">Kaggle SEC Financials database</a>.
   *
   * @param symbol       Symbol.
   * @param cik          CIK.
   * @param accessNumber Access number of a specific report you want to retrieve financials from.
   * @param freq         Frequency. Can be either <code>annual</code> or <code>quarterly</code>. Default to
   *                               <code>annual</code>.
   * @param from         From date <code>YYYY-MM-DD</code>. Filter for endDate.
   * @param to           To date <code>YYYY-MM-DD</code>. Filter for endDate.
   * @return Response from the API call
   */
  async financialsReported(
    symbol?: string,
    cik?: string,
    accessNumber?: string,
    freq?: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<FinancialsAsReported>> {
    const req = this.createRequest('GET', '/stock/financials-reported');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      cik: [cik, optional(string())],
      accessNumber: [accessNumber, optional(string())],
      freq: [freq, optional(string())],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('cik', mapped.cik);
    req.query('accessNumber', mapped.accessNumber);
    req.query('freq', mapped.freq);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(financialsAsReportedSchema, requestOptions);
  }

  /**
   * <p>Get revenue breakdown as-reporetd by product and geography. Users on personal plans can access
   * data for US companies which disclose their revenue breakdown in the annual or quarterly reports.
   * </p><p>Global standardized revenue breakdown/segments data is available for Enterprise users. <a
   * href="mailto:support@finnhub.io">Contact us</a> to inquire about the access for Global standardized
   * data.</p>
   *
   * @param symbol       Symbol.
   * @param cik          CIK.
   * @return Response from the API call
   */
  async revenueBreakdown(
    symbol?: string,
    cik?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RevenueBreakdown>> {
    const req = this.createRequest('GET', '/stock/revenue-breakdown');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      cik: [cik, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('cik', mapped.cik);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(revenueBreakdownSchema, requestOptions);
  }

  /**
   * List company's filing. Limit to 250 documents at a time. This data is available for bulk download on
   * <a href="https://www.kaggle.com/finnhub/sec-filings" target="_blank">Kaggle SEC Filings database</a>.
   *
   * @param symbol       Symbol. Leave <code>symbol</code>,<code>cik</code> and <code>accessNumber</code>
   *                               empty to list latest filings.
   * @param cik          CIK.
   * @param accessNumber Access number of a specific report you want to retrieve data from.
   * @param form         Filter by form. You can use this value <code>NT 10-K</code> to find non-timely
   *                               filings for a company.
   * @param from         From date: 2023-03-15.
   * @param to           To date: 2023-03-16.
   * @return Response from the API call
   */
  async filings(
    symbol?: string,
    cik?: string,
    accessNumber?: string,
    form?: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Filing[]>> {
    const req = this.createRequest('GET', '/stock/filings');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      cik: [cik, optional(string())],
      accessNumber: [accessNumber, optional(string())],
      form: [form, optional(string())],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('cik', mapped.cik);
    req.query('accessNumber', mapped.accessNumber);
    req.query('form', mapped.form);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(filingSchema), requestOptions);
  }

  /**
   * Get sentiment analysis of 10-K and 10-Q filings from SEC. An abnormal increase in the number of
   * positive/negative words in filings can signal a significant change in the company's stock price in
   * the upcoming 4 quarters. We make use of <a href= "https://sraf.nd.edu/textual-analysis/resources/"
   * target="_blank">Loughran and McDonald Sentiment Word Lists</a> to calculate the sentiment for each
   * filing.
   *
   * @param accessNumber Access number of a specific report you want to retrieve data from.
   * @return Response from the API call
   */
  async filingsSentiment(
    accessNumber: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SecSentimentAnalysis>> {
    const req = this.createRequest('GET', '/stock/filings-sentiment');
    const mapped = req.prepareArgs({ accessNumber: [accessNumber, string()] });
    req.header('Content-Type', 'application/json');
    req.query('accessNumber', mapped.accessNumber);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(secSentimentAnalysisSchema, requestOptions);
  }

  /**
   * <p>Calculate the textual difference between a company's 10-K / 10-Q reports and the same type of
   * report in the previous year using Cosine Similarity. For example, this endpoint compares 2019's 10-K
   * with 2018's 10-K. Companies breaking from its routines in disclosure of financial condition and risk
   * analysis section can signal a significant change in the company's stock price in the upcoming 4
   * quarters.</p>
   *
   * @param symbol       Symbol. Required if cik is empty
   * @param cik          CIK. Required if symbol is empty
   * @param freq         <code>annual</code> or <code>quarterly</code>. Default to <code>annual</code>
   * @return Response from the API call
   */
  async similarityIndex(
    symbol?: string,
    cik?: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SimilarityIndex>> {
    const req = this.createRequest('GET', '/stock/similarity-index');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      cik: [cik, optional(string())],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('cik', mapped.cik);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(similarityIndexSchema, requestOptions);
  }

  /**
   * Get recent and upcoming IPO.
   *
   * @param from         From date: 2020-03-15.
   * @param to           To date: 2020-03-16.
   * @return Response from the API call
   */
  async ipoCalendar(
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IpoCalendar>> {
    const req = this.createRequest('GET', '/calendar/ipo');
    const mapped = req.prepareArgs({
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(ipoCalendarSchema, requestOptions);
  }

  /**
   * Get ratios for different sectors and regions/indices.
   *
   * @param region       Region. A list of supported values for this field can be found <a href="https:
   *                               //docs.google.com/spreadsheets/d/1afedyv7yWJ-z7pMjaAZK-f6ENY3mI3EBCk95QffpoHw/edit?
   *                               usp=sharing" target="_blank">here</a>.
   * @return Response from the API call
   */
  async sectorMetric(
    region: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SectorMetric>> {
    const req = this.createRequest('GET', '/sector/metrics');
    const mapped = req.prepareArgs({ region: [region, string()] });
    req.header('Content-Type', 'application/json');
    req.query('region', mapped.region);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(sectorMetricSchema, requestOptions);
  }

  /**
   * Get dividends data for common stocks going back 30 years.
   *
   * @param symbol       Symbol.
   * @param from         YYYY-MM-DD.
   * @param to           YYYY-MM-DD.
   * @return Response from the API call
   */
  async stockDividends(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Dividends[]>> {
    const req = this.createRequest('GET', '/stock/dividend');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(dividendsSchema), requestOptions);
  }

  /**
   * Get latest analyst recommendation trends for a company.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @return Response from the API call
   */
  async recommendationTrends(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RecommendationTrend[]>> {
    const req = this.createRequest('GET', '/stock/recommendation');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(recommendationTrendSchema), requestOptions);
  }

  /**
   * Get latest price target consensus.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @return Response from the API call
   */
  async priceTarget(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PriceTarget>> {
    const req = this.createRequest('GET', '/stock/price-target');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(priceTargetSchema, requestOptions);
  }

  /**
   * Get latest stock upgrade and downgrade.
   *
   * @param symbol       Symbol of the company: AAPL. If left blank, the API will return latest stock
   *                               upgrades/downgrades.
   * @param from         From date: 2000-03-15.
   * @param to           To date: 2020-03-16.
   * @return Response from the API call
   */
  async upgradeDowngrade(
    symbol?: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<UpgradeDowngrade[]>> {
    const req = this.createRequest('GET', '/stock/upgrade-downgrade');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(upgradeDowngradeSchema), requestOptions);
  }

  /**
   * Get company's revenue estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyRevenueEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RevenueEstimates>> {
    const req = this.createRequest('GET', '/stock/revenue-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(revenueEstimatesSchema, requestOptions);
  }

  /**
   * Get company's ebitda estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyEbitdaEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EbitdaEstimates>> {
    const req = this.createRequest('GET', '/stock/ebitda-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(ebitdaEstimatesSchema, requestOptions);
  }

  /**
   * Get company's ebit estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyEbitEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EbitEstimates>> {
    const req = this.createRequest('GET', '/stock/ebit-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(ebitEstimatesSchema, requestOptions);
  }

  /**
   * Get company's net income estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyNetIncomeEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NetIncomeEstimates>> {
    const req = this.createRequest('GET', '/stock/net-income-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(netIncomeEstimatesSchema, requestOptions);
  }

  /**
   * Get company's pretax income estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyPretaxIncomeEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PretaxIncomeEstimates>> {
    const req = this.createRequest('GET', '/stock/pretax-income-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(pretaxIncomeEstimatesSchema, requestOptions);
  }

  /**
   * Get company's gross income estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyGrossIncomeEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GrossIncomeEstimates>> {
    const req = this.createRequest('GET', '/stock/gross-income-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(grossIncomeEstimatesSchema, requestOptions);
  }

  /**
   * Get company's Dividend per Share estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyDpsEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DpsEstimates>> {
    const req = this.createRequest('GET', '/stock/dps-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(dpsEstimatesSchema, requestOptions);
  }

  /**
   * Get company's free cash flow estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyFcfEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<FcfEstimates>> {
    const req = this.createRequest('GET', '/stock/fcf-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(fcfEstimatesSchema, requestOptions);
  }

  /**
   * Get company's capital expenditure estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyCapexEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CapexEstimates>> {
    const req = this.createRequest('GET', '/stock/capex-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(capexEstimatesSchema, requestOptions);
  }

  /**
   * Get company's operating cash flow estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyOcfEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<OcfEstimates>> {
    const req = this.createRequest('GET', '/stock/ocf-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(ocfEstimatesSchema, requestOptions);
  }

  /**
   * Get company's EPS estimates.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param freq         Can take 1 of the following values: <code>annual, quarterly</code>. Default to
   *                               <code>quarterly</code>
   * @return Response from the API call
   */
  async companyEpsEstimates(
    symbol: string,
    freq?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningsEstimates>> {
    const req = this.createRequest('GET', '/stock/eps-estimate');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(earningsEstimatesSchema, requestOptions);
  }

  /**
   * Get company historical quarterly earnings surprise going back to 2000.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param limit        Limit number of period returned. Leave blank to get the full history.
   * @return Response from the API call
   */
  async companyEarnings(
    symbol: string,
    limit?: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningResult[]>> {
    const req = this.createRequest('GET', '/stock/earnings');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      limit: [limit, optional(bigint())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('limit', mapped.limit);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(earningResultSchema), requestOptions);
  }

  /**
   * Get historical and coming earnings release. EPS and Revenue in this endpoint are non-GAAP, which
   * means they are adjusted to exclude some one-time or unusual items. This is the same data investors
   * usually react to and talked about on the media. Estimates are sourced from both sell-side and buy-
   * side analysts.
   *
   * @param from          From date: 2020-03-15.
   * @param to            To date: 2020-03-16.
   * @param symbol        Filter by symbol: AAPL.
   * @param international Set to <code>true</code> to include international markets. Default value is
   *                                 <code>false</code>
   * @return Response from the API call
   */
  async earningsCalendar(
    from?: string,
    to?: string,
    symbol?: string,
    international?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningsCalendar>> {
    const req = this.createRequest('GET', '/calendar/earnings');
    const mapped = req.prepareArgs({
      from: [from, optional(string())],
      to: [to, optional(string())],
      symbol: [symbol, optional(string())],
      international: [international, optional(boolean())],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.query('symbol', mapped.symbol);
    req.query('international', mapped.international);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(earningsCalendarSchema, requestOptions);
  }

  /**
   * <p>Get real-time quote data for US stocks. Constant polling is not recommended. Use websocket if you
   * need real-time updates.</p><p>Real-time stock prices for international markets are supported for
   * Enterprise clients via our partner's feed. <a href="mailto:support@finnhub.io">Contact Us</a> to
   * learn more.</p>
   *
   * @param symbol       Symbol
   * @return Response from the API call
   */
  async quote(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quote>> {
    const req = this.createRequest('GET', '/quote');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(quoteSchema, requestOptions);
  }

  /**
   * <p>Get candlestick data (OHLCV) for stocks.</p><p>Daily data will be adjusted for Splits. Intraday
   * data will remain unadjusted. Only 1 month of intraday will be returned at a time. If you need more
   * historical intraday data, please use the from and to params iteratively to request more data.</p>
   *
   * @param symbol       Symbol.
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @param from         UNIX timestamp. Interval initial value.
   * @param to           UNIX timestamp. Interval end value.
   * @return Response from the API call
   */
  async stockCandles(
    symbol: string,
    resolution: string,
    from: bigint,
    to: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<StockCandles>> {
    const req = this.createRequest('GET', '/stock/candle');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
      from: [from, bigint()],
      to: [to, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(stockCandlesSchema, requestOptions);
  }

  /**
   * <p>Get historical tick data for global exchanges.</p><p>For more historical tick data, you can visit
   * our bulk download page in the Dashboard <a target="_blank" href="/dashboard/download",>here</a> to
   * speed up the download process.</p><table class="table table-hover">
   * <thead>
   * <tr>
   * <th>Exchange</th>
   * <th>Segment</th>
   * <th>Delay</th>
   * </tr>
   * </thead>
   * <tbody>
   * <tr>
   * <td class="text-blue">US CTA/UTP</th>
   * <td>Full SIP</td>
   * <td>End-of-day</td>
   * </tr>
   * <tr>
   * <td class="text-blue">TSX</th>
   * <td><ul><li>TSX</li><li>TSX Venture</li><li>Index</li></ul></td>
   * <td>End-of-day</td>
   * </tr>
   * <tr>
   * <td class="text-blue">LSE</th>
   * <td><ul><li>London Stock Exchange (L)</li><li>LSE International (L)</li><li>LSE European
   * (L)</li></ul></td>
   * <td>15 minute</td>
   * </tr>
   * <tr>
   * <td class="text-blue">Euronext</th>
   * <td><ul> <li>Euronext Paris (PA)</li> <li>Euronext Amsterdam (AS)</li> <li>Euronext Lisbon
   * (LS)</li> <li>Euronext Brussels (BR)</li> <li>Euronext Oslo (OL)</li> <li>Euronext London (LN)</li>
   * <li>Euronext Dublin (IR)</li> <li>Index</li> <li>Warrant</li></ul></td>
   * <td>End-of-day</td>
   * </tr>
   * <tr>
   * <td class="text-blue">Deutsche Börse</th>
   * <td><ul> <li>Frankfurt (F)</li> <li>Xetra (DE)</li> <li>Duesseldorf (DU)</li> <li>Hamburg
   * (HM)</li> <li>Berlin (BE)</li> <li>Hanover (HA)</li> <li>Stoxx (SX)</li> <li>TradeGate (TG)</li>
   * <li>Zertifikate (SC)</li> <li>Index</li> <li>Warrant</li></ul></td>
   * <td>End-of-day</td>
   * </tr>
   * </tbody>
   * </table>
   *
   * @param symbol       Symbol.
   * @param date         Date: 2020-04-02.
   * @param limit        Limit number of ticks returned. Maximum value: <code>25000</code>
   * @param skip         Number of ticks to skip. Use this parameter to loop through the entire data.
   * @return Response from the API call
   */
  async stockTick(
    symbol: string,
    date: string,
    limit: bigint,
    skip: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TickData>> {
    const req = this.createRequest('GET', '/stock/tick');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      date: [date, string()],
      limit: [limit, bigint()],
      skip: [skip, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('date', mapped.date);
    req.query('limit', mapped.limit);
    req.query('skip', mapped.skip);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(tickDataSchema, requestOptions);
  }

  /**
   * <p>Get historical best bid and offer for US stocks, LSE, TSX, Euronext and Deutsche Borse.</p><p>For
   * US market, this endpoint only serves historical NBBO from the beginning of 2023. To download more
   * historical data, please visit our bulk download page in the Dashboard <a target="_blank"
   * href="/dashboard/download",>here</a>.</p>
   *
   * @param symbol       Symbol.
   * @param date         Date: 2020-04-02.
   * @param limit        Limit number of ticks returned. Maximum value: <code>25000</code>
   * @param skip         Number of ticks to skip. Use this parameter to loop through the entire data.
   * @return Response from the API call
   */
  async stockNbbo(
    symbol: string,
    date: string,
    limit: bigint,
    skip: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoricalNbbo>> {
    const req = this.createRequest('GET', '/stock/bbo');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      date: [date, string()],
      limit: [limit, bigint()],
      skip: [skip, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('date', mapped.date);
    req.query('limit', mapped.limit);
    req.query('skip', mapped.skip);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(historicalNbboSchema, requestOptions);
  }

  /**
   * Get last bid/ask data for US stocks.
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async stockBidask(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LastBidAsk>> {
    const req = this.createRequest('GET', '/stock/bidask');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(lastBidAskSchema, requestOptions);
  }

  /**
   * Get splits data for stocks.
   *
   * @param symbol       Symbol.
   * @param from         YYYY-MM-DD.
   * @param to           YYYY-MM-DD.
   * @return Response from the API call
   */
  async stockSplits(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Split[]>> {
    const req = this.createRequest('GET', '/stock/split');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(splitSchema), requestOptions);
  }

  /**
   * Get global dividends data.
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async stockBasicDividends(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Dividends2>> {
    const req = this.createRequest('GET', '/stock/dividend2');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(dividends2Schema, requestOptions);
  }

  /**
   * Get a list of index's constituents. A list of supported indices for this endpoint can be found <a
   * href="/api/v1/index/list?token=" target="_blank">here</a>.
   *
   * @param symbol       symbol
   * @return Response from the API call
   */
  async indicesConstituents(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IndicesConstituents>> {
    const req = this.createRequest('GET', '/index/constituents');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(indicesConstituentsSchema, requestOptions);
  }

  /**
   * Get full history of index's constituents including symbols and dates of joining and leaving the
   * Index. A list of supported indices for this endpoint can be found <a href="/api/v1/index/historical-
   * list?token=" target="_blank">here</a>.
   *
   * @param symbol       symbol
   * @return Response from the API call
   */
  async indicesHistoricalConstituents(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IndicesHistoricalConstituents>> {
    const req = this.createRequest('GET', '/index/historical-constituents');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(indicesHistoricalConstituentsSchema, requestOptions);
  }

  /**
   * Get ETF profile information. This endpoint has global coverage. A list of supported ETFs can be
   * found <a href="/api/v1/etf/list?type=csv&token=" target="_blank">here</a>.
   *
   * @param symbol       ETF symbol.
   * @param isin         ETF isin.
   * @return Response from the API call
   */
  async etfsProfile(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EtFsProfile>> {
    const req = this.createRequest('GET', '/etf/profile');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(etFsProfileSchema, requestOptions);
  }

  /**
   * Get full ETF holdings/constituents. This endpoint has global coverage. Widget only shows top 10
   * holdings. A list of supported ETFs can be found <a href="/api/v1/etf/list?token="
   * target="_blank">here</a>.
   *
   * @param symbol       ETF symbol.
   * @param isin         ETF isin.
   * @param skip         Skip the first n results. You can use this parameter to query historical
   *                               constituents data. The latest result is returned if skip=0 or not set.
   * @param date         Query holdings by date. You can use either this param or <code>skip</code> param,
   *                               not both.
   * @return Response from the API call
   */
  async etfsHoldings(
    symbol?: string,
    isin?: string,
    skip?: bigint,
    date?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EtFsHoldings>> {
    const req = this.createRequest('GET', '/etf/holdings');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
      skip: [skip, optional(bigint())],
      date: [date, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.query('skip', mapped.skip);
    req.query('date', mapped.date);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(etFsHoldingsSchema, requestOptions);
  }

  /**
   * Get ETF sector exposure data.
   *
   * @param symbol       ETF symbol.
   * @param isin         ETF isin.
   * @return Response from the API call
   */
  async etfsSectorExposure(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EtFsSectorExposure>> {
    const req = this.createRequest('GET', '/etf/sector');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(etFsSectorExposureSchema, requestOptions);
  }

  /**
   * Get ETF country exposure data.
   *
   * @param symbol       ETF symbol.
   * @param isin         ETF isin.
   * @return Response from the API call
   */
  async etfsCountryExposure(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EtFsCountryExposure>> {
    const req = this.createRequest('GET', '/etf/country');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(etFsCountryExposureSchema, requestOptions);
  }

  /**
   * Get ETF equity allocation based on the characteristics of the holdings.
   *
   * @param symbol       ETF symbol.
   * @param isin         ETF isin.
   * @return Response from the API call
   */
  async etfsAllocation(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EtFsAllocation>> {
    const req = this.createRequest('GET', '/etf/allocation');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(etFsAllocationSchema, requestOptions);
  }

  /**
   * Get mutual funds profile information. This endpoint covers both US and global mutual funds. For
   * international funds, you must query the data using ISIN. A list of supported funds can be found <a
   * href="/api/v1/mutual-fund/list?type=csv&token=" target="_blank">here</a>.
   *
   * @param symbol       Fund's symbol.
   * @param isin         Fund's isin.
   * @return Response from the API call
   */
  async mutualFundProfile(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundProfile>> {
    const req = this.createRequest('GET', '/mutual-fund/profile');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundProfileSchema, requestOptions);
  }

  /**
   * Get full Mutual Funds holdings/constituents. This endpoint covers both US and global mutual funds.
   * For international funds, you must query the data using ISIN. A list of supported funds can be found
   * <a href="/api/v1/mutual-fund/list?token=" target="_blank">here</a>.
   *
   * @param symbol       Fund's symbol.
   * @param isin         Fund's isin.
   * @param skip         Skip the first n results. You can use this parameter to query historical
   *                               constituents data. The latest result is returned if skip=0 or not set.
   * @return Response from the API call
   */
  async mutualFundHoldings(
    symbol?: string,
    isin?: string,
    skip?: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundHoldings>> {
    const req = this.createRequest('GET', '/mutual-fund/holdings');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
      skip: [skip, optional(bigint())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.query('skip', mapped.skip);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundHoldingsSchema, requestOptions);
  }

  /**
   * Get Mutual Funds sector exposure data.
   *
   * @param symbol       Mutual Fund symbol.
   * @param isin         Fund's isin.
   * @return Response from the API call
   */
  async mutualFundSectorExposure(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundSectorExposure>> {
    const req = this.createRequest('GET', '/mutual-fund/sector');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundSectorExposureSchema, requestOptions);
  }

  /**
   * Get Mutual Funds country exposure data.
   *
   * @param symbol       Symbol.
   * @param isin         Fund's isin.
   * @return Response from the API call
   */
  async mutualFundCountryExposure(
    symbol?: string,
    isin?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundCountryExposure>> {
    const req = this.createRequest('GET', '/mutual-fund/country');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      isin: [isin, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundCountryExposureSchema, requestOptions);
  }

  /**
   * Get EET data for EU funds. For PAIs data, please see the EET PAI endpoint.
   *
   * @param isin         ISIN.
   * @return Response from the API call
   */
  async mutualFundEet(
    isin: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundEet>> {
    const req = this.createRequest('GET', '/mutual-fund/eet');
    const mapped = req.prepareArgs({ isin: [isin, string()] });
    req.header('Content-Type', 'application/json');
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundEetSchema, requestOptions);
  }

  /**
   * Get EET PAI data for EU funds.
   *
   * @param isin         ISIN.
   * @return Response from the API call
   */
  async mutualFundEetPai(
    isin: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<MutualFundEetPai>> {
    const req = this.createRequest('GET', '/mutual-fund/eet-pai');
    const mapped = req.prepareArgs({ isin: [isin, string()] });
    req.header('Content-Type', 'application/json');
    req.query('isin', mapped.isin);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(mutualFundEetPaiSchema, requestOptions);
  }

  /**
   * List supported forex exchanges
   *
   * @return Response from the API call
   */
  async forexExchanges(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string[]>> {
    const req = this.createRequest('GET', '/forex/exchange');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(string()), requestOptions);
  }

  /**
   * List supported forex symbols.
   *
   * @param exchange     Exchange you want to get the list of symbols from.
   * @return Response from the API call
   */
  async forexSymbols(
    exchange: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ForexSymbol[]>> {
    const req = this.createRequest('GET', '/forex/symbol');
    const mapped = req.prepareArgs({ exchange: [exchange, string()] });
    req.header('Content-Type', 'application/json');
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(forexSymbolSchema), requestOptions);
  }

  /**
   * Get candlestick data for forex symbols.
   *
   * @param symbol       Use symbol returned in <code>/forex/symbol</code> endpoint for this field.
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @param from         UNIX timestamp. Interval initial value.
   * @param to           UNIX timestamp. Interval end value.
   * @return Response from the API call
   */
  async forexCandles(
    symbol: string,
    resolution: string,
    from: bigint,
    to: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ForexCandles>> {
    const req = this.createRequest('GET', '/forex/candle');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
      from: [from, bigint()],
      to: [to, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(forexCandlesSchema, requestOptions);
  }

  /**
   * Get rates for all forex pairs. Ideal for currency conversion
   *
   * @param base         Base currency. Default to EUR.
   * @param date         Date. Leave blank to get the latest data.
   * @return Response from the API call
   */
  async forexRates(
    base?: string,
    date?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Forexrates>> {
    const req = this.createRequest('GET', '/forex/rates');
    const mapped = req.prepareArgs({
      base: [base, optional(string())],
      date: [date, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('base', mapped.base);
    req.query('date', mapped.date);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(forexratesSchema, requestOptions);
  }

  /**
   * List supported crypto exchanges
   *
   * @return Response from the API call
   */
  async cryptoExchanges(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<string[]>> {
    const req = this.createRequest('GET', '/crypto/exchange');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(string()), requestOptions);
  }

  /**
   * List supported crypto symbols by exchange
   *
   * @param exchange     Exchange you want to get the list of symbols from.
   * @return Response from the API call
   */
  async cryptoSymbols(
    exchange: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CryptoSymbol[]>> {
    const req = this.createRequest('GET', '/crypto/symbol');
    const mapped = req.prepareArgs({ exchange: [exchange, string()] });
    req.header('Content-Type', 'application/json');
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(cryptoSymbolSchema), requestOptions);
  }

  /**
   * Get crypto's profile.
   *
   * @param symbol       Crypto symbol such as BTC or ETH.
   * @return Response from the API call
   */
  async cryptoProfile(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CryptoProfile>> {
    const req = this.createRequest('GET', '/crypto/profile');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(cryptoProfileSchema, requestOptions);
  }

  /**
   * Get candlestick data for crypto symbols.
   *
   * @param symbol       Use symbol returned in <code>/crypto/symbol</code> endpoint for this field.
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @param from         UNIX timestamp. Interval initial value.
   * @param to           UNIX timestamp. Interval end value.
   * @return Response from the API call
   */
  async cryptoCandles(
    symbol: string,
    resolution: string,
    from: bigint,
    to: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CryptoCandles>> {
    const req = this.createRequest('GET', '/crypto/candle');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
      from: [from, bigint()],
      to: [to, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(cryptoCandlesSchema, requestOptions);
  }

  /**
   * Run pattern recognition algorithm on a symbol. Support double top/bottom, triple top/bottom, head
   * and shoulders, triangle, wedge, channel, flag, and candlestick patterns.
   *
   * @param symbol       Symbol
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @return Response from the API call
   */
  async patternRecognition(
    symbol: string,
    resolution: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PatternRecognition>> {
    const req = this.createRequest('GET', '/scan/pattern');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(patternRecognitionSchema, requestOptions);
  }

  /**
   * Get support and resistance levels for a symbol.
   *
   * @param symbol       Symbol
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @return Response from the API call
   */
  async supportResistance(
    symbol: string,
    resolution: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SupportResistance>> {
    const req = this.createRequest('GET', '/scan/support-resistance');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(supportResistanceSchema, requestOptions);
  }

  /**
   * Get aggregate signal of multiple technical indicators such as MACD, RSI, Moving Average v.v. A full
   * list of indicators can be found <a href="https://docs.google.
   * com/spreadsheets/d/1MWuy0WuT2yVlxr1KbPdggVygMZtJfunDnhe-C0GEXYM/edit?usp=sharing"
   * target="_blank">here</a>.
   *
   * @param symbol       symbol
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @return Response from the API call
   */
  async aggregateIndicator(
    symbol: string,
    resolution: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AggregateIndicators>> {
    const req = this.createRequest('GET', '/scan/technical-indicator');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(aggregateIndicatorsSchema, requestOptions);
  }

  /**
   * Return technical indicator with price data. List of supported indicators can be found <a href="https:
   * //docs.google.com/spreadsheets/d/1ylUvKHVYN2E87WdwIza8ROaCpd48ggEl1k5i5SgA29k/edit?usp=sharing"
   * target="_blank">here</a>.
   *
   * @param symbol       symbol
   * @param resolution   Supported resolution includes <code>1, 5, 15, 30, 60, D, W, M </code>.Some
   *                               timeframes might not be available depending on the exchange.
   * @param from         UNIX timestamp. Interval initial value.
   * @param to           UNIX timestamp. Interval end value.
   * @param indicator    Indicator name. Full list can be found <a href="https://docs.google.
   *                               com/spreadsheets/d/1ylUvKHVYN2E87WdwIza8ROaCpd48ggEl1k5i5SgA29k/edit?usp=sharing"
   *                               target="_blank">here</a>.
   * @return Response from the API call
   */
  async technicalIndicator(
    symbol: string,
    resolution: string,
    from: bigint,
    to: bigint,
    indicator: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown | undefined>> {
    const req = this.createRequest('GET', '/indicator');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      resolution: [resolution, string()],
      from: [from, bigint()],
      to: [to, bigint()],
      indicator: [indicator, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('resolution', mapped.resolution);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.query('indicator', mapped.indicator);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(optional(unknown()), requestOptions);
  }

  /**
   * List earnings call transcripts' metadata. This endpoint is available for Global companies. You can
   * get a list of supported symbols <a target="_blank" href="/api/v1/stock/transcripts/symbol?
   * token=">here</a>
   *
   * @param symbol       Company symbol: AAPL. Leave empty to list the latest transcripts
   * @return Response from the API call
   */
  async transcriptsList(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningsCallTranscriptsList>> {
    const req = this.createRequest('GET', '/stock/transcripts/list');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(earningsCallTranscriptsListSchema, requestOptions);
  }

  /**
   * <p>Get earnings call transcripts, audio and participants' list. Data is available for US, UK,
   * European, Australian and Canadian companies.<p>15+ years of data is available with 220,000+ audio
   * which add up to 7TB in size.</p>
   *
   * @param id           Transcript's id obtained with <a href="#transcripts-list">Transcripts List
   *                               endpoint</a>.
   * @return Response from the API call
   */
  async transcripts(
    id: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningsCallTranscripts>> {
    const req = this.createRequest('GET', '/stock/transcripts');
    const mapped = req.prepareArgs({ id: [id, string()] });
    req.header('Content-Type', 'application/json');
    req.query('id', mapped.id);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(earningsCallTranscriptsSchema, requestOptions);
  }

  /**
   * <p>Stream live earnings calls with data provided in the calendar. The data will be available in m3u8
   * format. mp3 files will be available once the calls finish in the <code>recording</code> field.</p>
   *
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @param symbol       Filter by symbol: AAPL.
   * @return Response from the API call
   */
  async earningsCallLive(
    from?: string,
    to?: string,
    symbol?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EarningsCallLive>> {
    const req = this.createRequest('GET', '/stock/earnings-call-live');
    const mapped = req.prepareArgs({
      from: [from, optional(string())],
      to: [to, optional(string())],
      symbol: [symbol, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(earningsCallLiveSchema, requestOptions);
  }

  /**
   * <p>Get presentations/slides data in PDF format that are usually used during earnings calls. You can
   * get a list of supported symbols <a target="_blank" href="/api/v1/stock/presentation/symbol?
   * token=">here</a></p>
   *
   * @param symbol       Company symbol.
   * @return Response from the API call
   */
  async stockPresentation(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<StockPresentation>> {
    const req = this.createRequest('GET', '/stock/presentation');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(stockPresentationSchema, requestOptions);
  }

  /**
   * <p>Get social sentiment for stocks on Reddit and Twitter.</p>
   *
   * @param symbol       Company symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async socialSentiment(
    symbol: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SocialSentiment>> {
    const req = this.createRequest('GET', '/stock/social-sentiment');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(socialSentimentSchema, requestOptions);
  }

  /**
   * <p>Thematic investing involves creating a portfolio (or portion of a portfolio) by gathering
   * together a collection of companies involved in certain areas that you predict will generate above-
   * market returns over the long term. Themes can be based on a concept such as ageing populations or a
   * sub-sector such as robotics, and drones. Thematic investing focuses on predicted long-term trends
   * rather than specific companies or sectors, enabling investors to access structural, one-off shifts
   * that can change an entire industry.</p><p>This endpoint will help you get portfolios of different
   * investment themes that are changing our life and are the way of the future.</p><p>A full list of
   * themes supported can be found <a target="_blank" href="https://docs.google.
   * com/spreadsheets/d/1ULj9xDh4iPoQj279M084adZ2_S852ttRthKKJ7madYc/edit?usp=sharing">here</a>. The
   * theme coverage and portfolios are updated bi-weekly by our analysts. Our approach excludes penny,
   * super-small cap and illiquid stocks.</p>
   *
   * @param theme        Investment theme. A full list of themes supported can be found <a target="_blank"
   *                               href="https://docs.google.
   *                               com/spreadsheets/d/1ULj9xDh4iPoQj279M084adZ2_S852ttRthKKJ7madYc/edit?
   *                               usp=sharing">here</a>.
   * @return Response from the API call
   */
  async investmentThemes(
    theme: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InvestmentThemes>> {
    const req = this.createRequest('GET', '/stock/investment-theme');
    const mapped = req.prepareArgs({ theme: [theme, string()] });
    req.header('Content-Type', 'application/json');
    req.query('theme', mapped.theme);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(investmentThemesSchema, requestOptions);
  }

  /**
   * <p>This endpoint provides an overall map of public companies' key customers and suppliers. The data
   * offers a deeper look into a company's supply chain and how products are created. The data will help
   * investors manage risk, limit exposure or generate alpha-generating ideas and trading insights.</p>
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async supplyChainRelationships(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SupplyChainRelationships>> {
    const req = this.createRequest('GET', '/stock/supply-chain');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(supplyChainRelationshipsSchema, requestOptions);
  }

  /**
   * <p>This endpoint provides the latest ESG scores and important indicators for 7000+ global companies.
   * The data is collected through company's public ESG disclosure and public sources.</p><p>Our ESG
   * scoring models takes into account more than 150 different inputs to calculate the level of ESG risks
   * and how well a company is managing them. A higher score means lower ESG risk or better ESG
   * management. ESG scores are in the the range of 0-100. Some key indicators might contain letter-grade
   * score from C- to A+ with C- is the lowest score and A+ is the highest score.</p>
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async companyEsgScore(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyEsg>> {
    const req = this.createRequest('GET', '/stock/esg');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(companyEsgSchema, requestOptions);
  }

  /**
   * <p>This endpoint provides historical ESG scores and important indicators for 7000+ global companies.
   * The data is collected through company's public ESG disclosure and public sources.</p><p>Our ESG
   * scoring models takes into account more than 150 different inputs to calculate the level of ESG risks
   * and how well a company is managing them. A higher score means lower ESG risk or better ESG
   * management. ESG scores are in the the range of 0-100. Some key indicators might contain letter-grade
   * score from C- to A+ with C- is the lowest score and A+ is the highest score.</p>
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async companyHistoricalEsgScore(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoricalCompanyEsg>> {
    const req = this.createRequest('GET', '/stock/historical-esg');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(historicalCompanyEsgSchema, requestOptions);
  }

  /**
   * <p>This endpoint provides Earnings Quality Score for global companies.</p><p> Earnings quality
   * refers to the extent to which current earnings predict future earnings. "High-quality" earnings are
   * expected to persist, while "low-quality" earnings do not. A higher score means a higher earnings
   * quality</p><p>Finnhub uses a proprietary model which takes into consideration 4 criteria:</p> <ul
   * style="list-style-type: unset; margin-left: 30px;"><li>Profitability</li><li>Growth</li><li>Cash
   * Generation & Capital Allocation</li><li>Leverage</li></ul><br/><p>We then compare the metrics of
   * each company in each category against its peers in the same industry to gauge how quality its
   * earnings is.</p>
   *
   * @param symbol       Symbol.
   * @param freq         Frequency. Currently support <code>annual</code> and <code>quarterly</code>
   * @return Response from the API call
   */
  async companyEarningsQualityScore(
    symbol: string,
    freq: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CompanyEarningsQualityScore>> {
    const req = this.createRequest('GET', '/stock/earnings-quality-score');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      freq: [freq, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('freq', mapped.freq);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(companyEarningsQualityScoreSchema, requestOptions);
  }

  /**
   * Get real-time updates on the number of COVID-19 (Corona virus) cases in the US with a state-by-state
   * breakdown. Data is sourced from CDC and reputable sources. You can also access this API <a
   * href="https://rapidapi.com/Finnhub/api/finnhub-real-time-covid-19" target="_blank"
   * rel="nofollow">here</a>
   *
   * @return Response from the API call
   */
  async covid19(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CovidInfo[]>> {
    const req = this.createRequest('GET', '/covid19/us');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(covidInfoSchema), requestOptions);
  }

  /**
   * FDA's advisory committees are established to provide functions which support the agency's mission of
   * protecting and promoting the public health, while meeting the requirements set forth in the Federal
   * Advisory Committee Act. Committees are either mandated by statute or established at the discretion
   * of the Department of Health and Human Services. Each committee is subject to renewal at two-year
   * intervals unless the committee charter states otherwise.
   *
   * @return Response from the API call
   */
  async fdaCommitteeMeetingCalendar(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<FdaComitteeMeeting[]>> {
    const req = this.createRequest('GET', '/fda-advisory-committee-calendar');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(fdaComitteeMeetingSchema), requestOptions);
  }

  /**
   * List USPTO patents for companies. Limit to 250 records per API call.
   *
   * @param symbol       Symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async stockUsptoPatent(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<UsptoPatentResult>> {
    const req = this.createRequest('GET', '/stock/uspto-patent');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(usptoPatentResultSchema, requestOptions);
  }

  /**
   * Get a list of H1-B and Permanent visa applications for companies from the DOL. The data is updated
   * quarterly.
   *
   * @param symbol       Symbol.
   * @param from         From date <code>YYYY-MM-DD</code>. Filter on the <code>beginDate</code> column.
   * @param to           To date <code>YYYY-MM-DD</code>. Filter on the <code>beginDate</code> column.
   * @return Response from the API call
   */
  async stockVisaApplication(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<VisaApplicationResult>> {
    const req = this.createRequest('GET', '/stock/visa-application');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(visaApplicationResultSchema, requestOptions);
  }

  /**
   * Get a list of reported lobbying activities in the Senate and the House.
   *
   * @param symbol       Symbol.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async stockLobbying(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LobbyingResult>> {
    const req = this.createRequest('GET', '/stock/lobbying');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(lobbyingResultSchema, requestOptions);
  }

  /**
   * <p>Get a list of government's spending activities from USASpending dataset for public companies.
   * This dataset can help you identify companies that win big government contracts which is extremely
   * important for industries such as Defense, Aerospace, and Education. Only recent data is available
   * via the API.</p><p>For historical data, you can download it here: <a href="/api/v1/stock/usa-
   * spending?fileId=before_2021&token=" target="_blank">Pre-2021</a>, <a href="/api/v1/stock/usa-
   * spending?fileId=2021&token=" target="_blank">2021</a>, <a href="/api/v1/stock/usa-spending?
   * fileId=2022&token=" target="_blank">2022</a>, <a href="/api/v1/stock/usa-spending?
   * fileId=2023&token=" target="_blank">2023</a>, <a href="/api/v1/stock/usa-spending?
   * fileId=2024&token=" target="_blank">2024</a></p>
   *
   * @param symbol       Symbol.
   * @param from         From date <code>YYYY-MM-DD</code>. Filter for <code>actionDate</code>
   * @param to           To date <code>YYYY-MM-DD</code>. Filter for <code>actionDate</code>
   * @return Response from the API call
   */
  async stockUsaSpending(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<UsaSpendingResult>> {
    const req = this.createRequest('GET', '/stock/usa-spending');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(usaSpendingResultSchema, requestOptions);
  }

  /**
   * Get stock trades data disclosed by members of congress.
   *
   * @param symbol       Symbol of the company: AAPL.
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async congressionalTrading(
    symbol: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CongressionalTrading>> {
    const req = this.createRequest('GET', '/stock/congressional-trading');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(congressionalTradingSchema, requestOptions);
  }

  /**
   * Get general information of a bond. You can query by FIGI, ISIN or CUSIP. A list of supported bonds
   * can be found <a href="/api/v1/bond/list?type=csv&token=" target="_blank">here</a>.
   *
   * @param isin         ISIN
   * @param cusip        CUSIP
   * @param figi         FIGI
   * @return Response from the API call
   */
  async bondProfile(
    isin?: string,
    cusip?: string,
    figi?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BondProfile>> {
    const req = this.createRequest('GET', '/bond/profile');
    const mapped = req.prepareArgs({
      isin: [isin, optional(string())],
      cusip: [cusip, optional(string())],
      figi: [figi, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('isin', mapped.isin);
    req.query('cusip', mapped.cusip);
    req.query('figi', mapped.figi);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(bondProfileSchema, requestOptions);
  }

  /**
   * <p>Get bond's price data. The following datasets are supported:</p><table class="table table-
   * hover">
   * <thead>
   * <tr>
   * <th>Exchange</th>
   * <th>Segment</th>
   * <th>Delay</th>
   * </tr>
   * </thead>
   * <tbody>
   * <tr>
   * <td class="text-blue">US Government Bonds</td>
   * <td>Government Bonds</td>
   * <td>End-of-day</td>
   * </tr>
   * <tr>
   * <td class="text-blue">FINRA Trace</td>
   * <td>BTDS: US Corporate Bonds</td>
   * <td>Delayed 4h</td>
   * </tr>
   * <tr>
   * <td class="text-blue">FINRA Trace</td>
   * <td>144A Bonds</td>
   * <td>Delayed 4h</td>
   * </tr>
   * <tr>
   * <td class="text-blue">International Bonds</td>
   * <td>International Bonds</td>
   * <td>End-of-day</td>
   * </tr>
   * </tbody>
   * </table>
   *
   * @param isin         ISIN.
   * @param from         UNIX timestamp. Interval initial value.
   * @param to           UNIX timestamp. Interval end value.
   * @return Response from the API call
   */
  async bondPrice(
    isin: string,
    from: bigint,
    to: bigint,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BondCandles>> {
    const req = this.createRequest('GET', '/bond/price');
    const mapped = req.prepareArgs({
      isin: [isin, string()],
      from: [from, bigint()],
      to: [to, bigint()],
    });
    req.header('Content-Type', 'application/json');
    req.query('isin', mapped.isin);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(bondCandlesSchema, requestOptions);
  }

  /**
   * <p>Get trade-level data for bonds. The following datasets are supported:</p><table class="table
   * table-hover">
   * <thead>
   * <tr>
   * <th>Exchange</th>
   * <th>Segment</th>
   * <th>Delay</th>
   * </tr>
   * </thead>
   * <tbody>
   * <tr>
   * <td class="text-blue">FINRA Trace</th>
   * <td>BTDS: US Corporate Bonds</td>
   * <td>Delayed 4h</td>
   * </tr>
   * <tr>
   * <td class="text-blue">FINRA Trace</th>
   * <td>144A Bonds</td>
   * <td>Delayed 4h</td>
   * </tr>
   * </tbody>
   * </table>
   *
   * @param isin         ISIN.
   * @param date         Date: 2020-04-02.
   * @param limit        Limit number of ticks returned. Maximum value: <code>25000</code>
   * @param skip         Number of ticks to skip. Use this parameter to loop through the entire data.
   * @param exchange     Currently support the following values: <code>trace</code>.
   * @return Response from the API call
   */
  async bondTick(
    isin: string,
    date: string,
    limit: bigint,
    skip: bigint,
    exchange: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BondTickData>> {
    const req = this.createRequest('GET', '/bond/tick');
    const mapped = req.prepareArgs({
      isin: [isin, string()],
      date: [date, string()],
      limit: [limit, bigint()],
      skip: [skip, bigint()],
      exchange: [exchange, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('isin', mapped.isin);
    req.query('date', mapped.date);
    req.query('limit', mapped.limit);
    req.query('skip', mapped.skip);
    req.query('exchange', mapped.exchange);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(bondTickDataSchema, requestOptions);
  }

  /**
   * Get yield curve data for Treasury bonds.
   *
   * @param code         Bond's code. You can find the list of supported code <a href="https://docs.google.
   *                               com/spreadsheets/d/1iA-lM0Kht7lsQZ7Uu_s6r2i1BbQNUNO9eGkO5-zglHg/edit?usp=sharing"
   *                               target="_blank" rel="noopener">here</a>.
   * @return Response from the API call
   */
  async bondYieldCurve(
    code: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BondYieldCurve>> {
    const req = this.createRequest('GET', '/bond/yield-curve');
    const mapped = req.prepareArgs({ code: [code, string()] });
    req.header('Content-Type', 'application/json');
    req.query('code', mapped.code);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(bondYieldCurveSchema, requestOptions);
  }

  /**
   * Retrieve list of US bank branches information for a given symbol.
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async bankBranch(
    symbol: unknown,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankBranchRes>> {
    const req = this.createRequest('GET', '/bank-branch');
    const mapped = req.prepareArgs({ symbol: [symbol, optional(unknown())] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(bankBranchResSchema, requestOptions);
  }

  /**
   * <p>The Flight Ticket Price Index API provides comprehensive data on airline ticket prices, including
   * the average daily ticket price and its percentage change (price index). This data, collected weekly
   * and projected two weeks ahead, aggregates daily prices and indexes from the 50 busiest and largest
   * airports across the USA. The dataset includes detailed information on airlines, dates, and average
   * ticket prices, offering valuable insights for market analysis and pricing strategies.</p><p>The
   * price index is calculated as percentage change of average daily ticket price from the previous
   * weekly reading. Raw ticket prices data is available for Enterprise users. <a href="mailto:
   * support@finnhub.io">Contact us</a> to inquire about the raw price data.</p>
   *
   * @param airline      Filter data by airline. Accepted values: <code>united</code>,<code>delta</code>,
   *                               <code>american_airlines</code>,<code>southwest</code>,
   *                               <code>southern_airways_express</code>,<code>alaska_airlines</code>,
   *                               <code>frontier_airlines</code>,<code>jetblue_airways</code>,
   *                               <code>spirit_airlines</code>,<code>sun_country_airlines</code>,
   *                               <code>breeze_airways</code>,<code>hawaiian_airlines</code>
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async airlinePriceIndex(
    airline: string,
    from: string,
    to: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AirlinePriceIndexData>> {
    const req = this.createRequest('GET', '/airline/price-index');
    const mapped = req.prepareArgs({
      airline: [airline, string()],
      from: [from, string()],
      to: [to, string()],
    });
    req.header('Content-Type', 'application/json');
    req.query('airline', mapped.airline);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(airlinePriceIndexDataSchema, requestOptions);
  }

  /**
   * List all countries and metadata.
   *
   * @return Response from the API call
   */
  async country(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CountryMetadata[]>> {
    const req = this.createRequest('GET', '/country');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(countryMetadataSchema), requestOptions);
  }

  /**
   * <p>Get recent and upcoming economic releases.</p><p>Historical events and surprises are available
   * for Enterprise clients.</p>
   *
   * @param from         From date <code>YYYY-MM-DD</code>.
   * @param to           To date <code>YYYY-MM-DD</code>.
   * @return Response from the API call
   */
  async economicCalendar(
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EconomicCalendar>> {
    const req = this.createRequest('GET', '/calendar/economic');
    const mapped = req.prepareArgs({
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(economicCalendarSchema, requestOptions);
  }

  /**
   * List codes of supported economic data.
   *
   * @return Response from the API call
   */
  async economicCode(
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EconomicCode[]>> {
    const req = this.createRequest('GET', '/economic/code');
    req.header('Content-Type', 'application/json');
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(economicCodeSchema), requestOptions);
  }

  /**
   * Get economic data.
   *
   * @param code         Economic code.
   * @return Response from the API call
   */
  async economicData(
    code: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<EconomicData>> {
    const req = this.createRequest('GET', '/economic');
    const mapped = req.prepareArgs({ code: [code, string()] });
    req.header('Content-Type', 'application/json');
    req.query('code', mapped.code);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(economicDataSchema, requestOptions);
  }

  /**
   * List filings for international companies. Limit to 500 documents at a time. These are the documents
   * we use to source our fundamental data. Enterprise clients who need access to the full filings for
   * global markets should contact us for the access.
   *
   * @param symbol       Symbol. Leave empty to list latest filings.
   * @param country      Filter by country using country's 2-letter code.
   * @param from         From date: 2023-01-15.
   * @param to           To date: 2023-12-16.
   * @return Response from the API call
   */
  async internationalFilings(
    symbol?: string,
    country?: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InternationalFiling[]>> {
    const req = this.createRequest('GET', '/stock/international-filings');
    const mapped = req.prepareArgs({
      symbol: [symbol, optional(string())],
      country: [country, optional(string())],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('country', mapped.country);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(array(internationalFilingSchema), requestOptions);
  }

  /**
   * <p>Get standardized revenue breakdown and KPIs data for 30,000+ global companies.</p>
   *
   * @param symbol       Symbol.
   * @return Response from the API call
   */
  async revenueBreakdown2(
    symbol: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RevenueBreakdown2>> {
    const req = this.createRequest('GET', '/stock/revenue-breakdown2');
    const mapped = req.prepareArgs({ symbol: [symbol, string()] });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(revenueBreakdown2Schema, requestOptions);
  }

  /**
   * <p>Get latest articles posted directly on the companies' newsroom and investor relations page.
   * Newsroom API along with the Press Releases API provide a comprehensive text-based dataset directly
   * from the company. We currently cover 1,250 US Companies with this dataset.</p>
   *
   * @param symbol       Company symbol.
   * @param from         From time: 2025-01-01.
   * @param to           To time: 2026-01-05.
   * @return Response from the API call
   */
  async newsroom(
    symbol: string,
    from?: string,
    to?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Newsroom>> {
    const req = this.createRequest('GET', '/stock/newsroom');
    const mapped = req.prepareArgs({
      symbol: [symbol, string()],
      from: [from, optional(string())],
      to: [to, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('symbol', mapped.symbol);
    req.query('from', mapped.from);
    req.query('to', mapped.to);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(newsroomSchema, requestOptions);
  }

  /**
   * <p>Chat with our AI copilot trained on the extensive Finnhub's global data. You can ask it any
   * finance-related questions just like with other LLM models and receive results in texts and widgets.
   * </p>
   *
   * @param search       Search body
   * @return Response from the API call
   */
  async aiChat(
    search?: AiChatBody,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AiChatResponse>> {
    const req = this.createRequest('POST', '/ai-chat');
    const mapped = req.prepareArgs({
      search: [search, optional(aiChatBodySchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.search);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(aiChatResponseSchema, requestOptions);
  }

  /**
   * <p>Search for best-matched filings across global companies' filings, transcripts and press releases.
   * You can filter by anything from symbol, ISIN to form type, and document sources.</p><p>This endpoint
   * will return a list of documents that match your search criteria. If you would like to get the
   * excerpts as well, please set <code>highlighted</code> to <code>true</code>. Once you have the list
   * of documents, you can get a list of excerpts and positions to highlight the document using the
   * <code>/search-in-filing</code> endpoint</p>
   *
   * @param search       Search body
   * @return Response from the API call
   */
  async globalFilingsSearch(
    search?: SearchBody,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SearchResponse>> {
    const req = this.createRequest('POST', '/global-filings/search');
    const mapped = req.prepareArgs({
      search: [search, optional(searchBodySchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.search);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(searchResponseSchema, requestOptions);
  }

  /**
   * <p>Get a list of excerpts and highlight positions within a document using your query.</p>
   *
   * @param search       Search body
   * @return Response from the API call
   */
  async searchInFiling(
    search?: InFilingSearchBody,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InFilingResponse>> {
    const req = this.createRequest('POST', '/global-filings/search-in-filing');
    const mapped = req.prepareArgs({
      search: [search, optional(inFilingSearchBodySchema)],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.search);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(inFilingResponseSchema, requestOptions);
  }

  /**
   * <p>Get available values for each filter in search body.</p>
   *
   * @param field        Field to get available filters. Available filters are "countries", "exchanges",
   *                               "exhibits", "forms", "gics", "naics", "caps", "acts", and "sort".
   * @param source       Get available forms for each source.
   * @return Response from the API call
   */
  async globalFilingsSearchFilter(
    field: string,
    source?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SearchFilter>> {
    const req = this.createRequest('GET', '/global-filings/filter');
    const mapped = req.prepareArgs({
      field: [field, string()],
      source: [source, optional(string())],
    });
    req.header('Content-Type', 'application/json');
    req.query('field', mapped.field);
    req.query('source', mapped.source);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(searchFilterSchema, requestOptions);
  }

  /**
   * <p>Download filings using document ids.</p>
   *
   * @param documentId   Document's id. Note that this is different from filingId as 1 filing can contain
   *                               multiple documents.
   * @return Response from the API call
   */
  async globalFilingsDownload(
    documentId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest('GET', '/global-filings/download');
    const mapped = req.prepareArgs({ documentId: [documentId, string()] });
    req.header('Content-Type', 'application/json');
    req.query('documentId', mapped.documentId);
    req.authenticate([{ apiKey: true }]);
    return req.callAsJson(unknown(), requestOptions);
  }
}
